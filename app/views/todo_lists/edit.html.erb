<%= turbo_frame_tag dom_id(@todo_list) do %>
  <div class="card">
    <div class="card__body-inner">
      <p class="form-heading">Edit List</p>
      <%= render 'form', todo_list: @todo_list %>
      <%= link_to "Cancel", todo_lists_path, class: "btn btn--ghost" %>
    </div>
  </div>
<% end %>
